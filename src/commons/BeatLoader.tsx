import BeatLoader from 'react-spinners/BeatLoader';
import '@styles/commons/spinner.scss';

const LoadingSpinner = () => {
  return <BeatLoader color='#fff' className='loading-spinner' />;
};

export default LoadingSpinner;
